import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    sdButtonStyle: {
        backgroundColor: '#D9D9D9',
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#000',
        width: "100%",
        height: 50,
    }

});